<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingluo.dao.ArticleDao">

    <sql id="tb">article</sql>

    <!--  <select id="queryById" parameterType="com.lingluo.model.Article" useGeneratedKeys="true" keyProperty="articleId">
         select * from
        <include refid="tb"/>
        (article_id, article_title,
 
        article_content)
        values (#{articleId,jdbcType=INTEGER}, #{articleTitle,jdbcType=VARCHAR})
    </select> -->
    <insert id="save" parameterType="com.lingluo.model.Article">
        insert into
        <include refid="tb"/>
 
        (title,content,category_id,create_date,update_date,delete_date,create_user,visit_count,comment_state,state,permission,reminder,reminder_key)
         values ( #{title,jdbcType=VARCHAR},#{content,jdbcType=LONGVARCHAR},#{categoryId,jdbcType=TINYINT},#{createDate,jdbcType=TIMESTAMP},#{updateDate,jdbcType=TIMESTAMP},#{deleteDate,jdbcType=TIMESTAMP},#{createUser,jdbcType=SMALLINT},#{visitCount,jdbcType=INTEGER},#{commentState,jdbcType=TINYINT},#{state,jdbcType=TINYINT},#{permission,jdbcType=TINYINT},#{reminder,jdbcType=VARCHAR},#{reminderKey,jdbcType=VARCHAR}
         )
    </insert>
    <select id="findById" parameterType="String" resultType="com.lingluo.model.Article">
        select * from 
        <include refid="tb"/>
 
        where id=#{id}
    </select>

    <select id="findByPage" resultType="com.lingluo.model.Article">
        select * from 
        <include refid="tb"/>
 
        limit #{page},#{size}
    </select>

    <select id="findAll" resultType="com.lingluo.model.Article">
        select * from 
        <include refid="tb"/>
    </select>
    <select id="count" resultType="Integer">
        select COUNT(id) from 
        <include refid="tb"/>
    </select>
    <delete id="delete" parameterType="String">
        delete 
        <include refid="tb"/>
        where id=#{id}
    </delete>
</mapper>